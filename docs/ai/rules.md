# AI Rules

- Do NOT assume full repo knowledge
- Only use provided files or tools
- Prefer minimal queries
- Avoid full-system scans
- Always propose plan before code
- Use only JSON object search syntax (`search={...}` with operators)
- Do not use legacy `field:value` search expressions
- Apply `OR` only inside `search.quick`; apply `AND` at root `search` level